apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-get-ui
  namespace: microtodo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: todo-get-ui
  template:
    metadata:
      labels:
        app: todo-get-ui
    spec:
      containers:
      - name: todo-get-ui
        image: acraadibhai.azurecr.io/gettask-todo-ui:v1
        ports:
        - containerPort: 80
        env:
          - name: CONNECTION_STRING
            value: "Driver={ODBC Driver 18 for SQL Server};Server=tcp:aadiserver.database.windows.net,1433;Database=aadi-db;Uid=useradmin;Pwd={your_password_here};Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"                                      

